<Window x:Class="LibraryApp.Presentation.LibraryView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Library" Height="450" Width="800">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*" />
            <ColumnDefinition Width="3*" />
        </Grid.ColumnDefinitions>

        <!-- Master -->
        <ListView ItemsSource="{Binding Catalog}" SelectedItem="{Binding SelectedProduct}">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="ID" DisplayMemberBinding="{Binding Id}" />
                    <GridViewColumn Header="Title" DisplayMemberBinding="{Binding Name}" />
                    <GridViewColumn Header="Qty" DisplayMemberBinding="{Binding Quantity}" />
                </GridView>
            </ListView.View>
        </ListView>

        <!-- Detail -->
        <StackPanel Grid.Column="1" Margin="10" DataContext="{Binding SelectedProduct}">
            <TextBlock Text="Details:" FontWeight="Bold" />
            <TextBlock Text="{Binding Name}" FontSize="16" />
            <TextBlock Text="{Binding Quantity}" FontSize="16" Margin="0,10,0,0" />

            <Button Content="Borrow" Command="{Binding BorrowBookCommand}" Margin="0,20,0,0"/>
        </StackPanel>
    </Grid>
</Window>
