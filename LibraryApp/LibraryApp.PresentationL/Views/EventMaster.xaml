<UserControl x:Class="LibraryApp.PresentationL.Views.EventMaster"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:LibraryApp.PresentationL.Views"
             xmlns:vmodel="clr-namespace:LibraryApp.PresentationL.ViewModel"
             xmlns:view="clr-namespace:LibraryApp.PresentationL.Views"
             mc:Ignorable="d" 
             Height="500" Width="800">
    <UserControl.DataContext>
        <vmodel:VMEventList/>
    </UserControl.DataContext>
    <DockPanel Margin="20">
        <TextBlock FontSize="24" DockPanel.Dock="Top" Margin="0,0,0,10">Events</TextBlock>
        <Grid VerticalAlignment="Stretch">
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <GroupBox Header="Selected Entry">
                <view:EventEntry FontSize="12" DataContext="{Binding SelectedVM}" Margin="0,0,9,0" />
            </GroupBox>

            <StackPanel Orientation="Vertical" HorizontalAlignment="Left" Margin="20,0,0,0" Grid.Row="0">
                <Button Command="{Binding SellCommand}" Margin="0,5">Sell</Button>
                <Button Command="{Binding SupplyCommand}" Margin="0,5">Supply</Button>
                <Button Command="{Binding ReturnCommand}" Margin="0,5">Return</Button>
                <Button Command="{Binding DeleteCommand}" Margin="0,5">Delete</Button>
                <Button Command="{Binding RefreshCommand}" Margin="0,5">Refresh</Button>
            </StackPanel>

            <GroupBox Grid.Row="1" Header="Event List" Margin="0,10,0,0">
                <ListView ItemsSource="{Binding EventList}" SelectedIndex="{Binding SelectedCatalog}">
                    <ListView.View>
                        <GridView>
                            <GridViewColumn Header="Id" Width="100" DisplayMemberBinding="{Binding Id}"/>
                            <GridViewColumn Header="Description" Width="200" DisplayMemberBinding="{Binding Description}"/>
                            <GridViewColumn Header="Timestamp" Width="150" DisplayMemberBinding="{Binding Timestamp}"/>
                        </GridView>
                    </ListView.View>
                </ListView>
            </GroupBox>
        </Grid>
    </DockPanel>
</UserControl>
