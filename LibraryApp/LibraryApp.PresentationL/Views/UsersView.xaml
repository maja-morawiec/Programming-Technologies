<UserControl x:Class="LibraryApp.PresentationL.Views.UsersView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:LibraryApp.PresentationL.ViewModels"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <UserControl.DataContext>
        <vm:UsersViewModel />
    </UserControl.DataContext>

    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*" />
            <ColumnDefinition Width="3*" />
        </Grid.ColumnDefinitions>

        <!-- Master -->
        <ListBox ItemsSource="{Binding Users}"
                 SelectedItem="{Binding SelectedUser}"
                 DisplayMemberPath="Name" />

        <!-- Detail -->
        <StackPanel Grid.Column="1" Margin="20,0,0,0">
            <TextBlock Text="Szczegóły użytkownika" FontWeight="Bold" FontSize="14"/>
            <TextBox Text="{Binding SelectedUser.Name, Mode=TwoWay}" Margin="0,10,0,0"/>
            <Button Content="Zapisz" Command="{Binding SaveUserCommand}" Margin="0,10,0,0"/>
        </StackPanel>
    </Grid>
</UserControl>
